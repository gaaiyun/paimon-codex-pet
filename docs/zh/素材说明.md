# 素材说明

当前版本是非像素的高级卡通贴纸风。固定识别元素为白色短发、星形发饰、纯净金色光环、半框眼镜、发间铅笔、黑金键盘，以及键盘右后角防洒杯架里的带盖保温杯。

正式加载入口是 `pet/paimon/pet.json` 与 `pet/paimon/spritesheet.webp`。`assets/`、`source/` 和 `qa/` 用于展示、复验与后续重建，不会被 `pet.json` 直接加载。

动作数量仍遵循固定 8×9 atlas；增加动作变化时应在既定 57 帧内调整姿态和表情，不增加 atlas 行，也不修改由 Codex 应用控制的播放速度。
